apiVerstion: machinelearning.seldon.io/v1
kind: SeldonDeployment
metadata: 
  name: wine-model
  namespace: seldon
spec:
  name: wines
  predictors:
  - graph:
      implementation: MLFLOW_SERVER
      modelUri: s3://my_bucket/path/to/model
      name: classifier
    name: default
    replicas: 1
